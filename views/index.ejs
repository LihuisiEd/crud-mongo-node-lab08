
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title>CRUD de Usuarios</title>
  <style>
    /* Agregue aquí sus estilos personalizados */
  </style>
</head>
<body>
  
    <div class="mx-auto p-4">
      <div class="mx-auto py-8">
        <h1 class="text-3xl text-center font-bold mb-6">¡Agregar Usuarios!</h1>
          <form action="/users" method="POST" class="max-w-5xl mx-auto bg-white rounded-lg p-6">
              <div class="mb-4">
                  <label for="name" class="block text-gray-700 text-sm font-bold mb-2">Nombre</label>
                  <input id="name" name="name" type="text" class="border border-gray-300 rounded-lg py-2 px-4 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent w-full md:w-1/2">
                  
                </div>
              <div class="mb-4">
                  <label for="email" class="block text-gray-700 text-sm font-bold mb-2">Email</label>
                  <input id="email" name="email" type="email" class="border border-gray-300 rounded-lg py-2 px-4 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent w-full md:w-1/2">
                  
                </div>
              <div class="mb-4">
                  <label for="password" class="block text-gray-700 text-sm font-bold mb-2">Contraseña</label>
                  <input id="password" name="password" type="password" class="border border-gray-300 rounded-lg py-2 px-4 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent w-full md:w-1/2">
                  
                </div>
              <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50">Registrar</button>
          </form>
      </div>

      <div class="mx-auto p-4 max-w-5xl mx-auto bg-white rounded-lg p-6 shadow-md w-full md:w-1/2">
        <h2 class="text-2xl font-bold mb-4">Lista de Usuarios</h2>
        <ul class="space-y-4">
          <% users.forEach((user, index) => { %>
          <li class="flex items-center justify-between">
            <div>
              <div class="flex items-center space-x-2">
                <p class="text-lg font-semibold"><%= user.name %></p>
                <p class="text-gray-600"><%= user.email %></p>
              </div>
            </div>
            <div class="mt-4 mb-4 space-y-2">
              <a href="/users/edit/<%= user._id %>" class="flex-1 px-4 py-2 bg-blue-500 text-white rounded-lg mr-2">Editar</a>
              <a href="/users/delete/<%= user._id %>" class="flex-1 px-4 py-2 bg-red-500 text-white rounded-lg mr-2">Eliminar</a>
            </div>
            <% if (index !== users.length - 1) { %>
              <hr class="mt-4 mb-4 border-gray-300 my-4">
            <% } %>
          </li>
          <% }) %>
        </ul>
        
    </div>
    
    
        
          
    </div>
    
  
</body>
</html>



<!--
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="/styles.css">
    <title>Título de la página</title>
</head>
<body>

    <div class="container mx-auto p-4">
        <h1 class="text-4xl font-bold mb-4">¡Hola, Tailwind!</h1>
        <p class="text-lg text-gray-700">Este es un ejemplo de una página HTML con Tailwind CSS.</p>
        <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 mt-4 rounded">Botón</button>
    </div>
    
</body>
</html>

-->


